import type { DocsNavSection } from '../../../types';
import { landingPage } from './landing';

export const overviewSection: DocsNavSection = {
  id: 'overview',
  pages: [landingPage],
  title: 'Overview',
};
