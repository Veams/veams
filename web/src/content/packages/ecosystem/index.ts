import type { DocsPackage } from '../../types';
import { overviewSection } from './overview';

export const ecosystemPackage: DocsPackage = {
  accent: 'teal',
  description: 'Structure, state, and hydration for modern frontends.',
  id: 'ecosystem',
  sections: [overviewSection],
  title: 'Ecosystem',
};
