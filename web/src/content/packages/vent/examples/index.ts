import type { DocsNavSection } from '../../../types';
import { exampleReleaseBusPage } from './example-release-bus';

export const examplesSection: DocsNavSection = {
  id: 'examples',
  pages: [exampleReleaseBusPage],
  title: 'Examples',
};
